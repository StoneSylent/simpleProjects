<!doctype html>
<html class="no-js" lang="en" itemscope itemtype="http://schema.org/Product"> <!--<![endif]-->
<head>
  <title>bestHomePage</title>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "libs/modernizr-2.6.2.min.js" %>
  <%= csrf_meta_tags %>

	<meta charset="utf-8">

	<!-- Use the .htaccess and remove these lines to avoid edge case issues.
			 More info: h5bp.com/b/378 -->
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
<% if (session[:user_id]) %>  
    <style>
    body {
        font-family:<%= Setting.find_by(user_name: session[:user_id]).fontFamily%>;
      font-size:<%= Setting.find_by(user_name: session[:user_id]).fontSize%>;
      color:<%= Setting.find_by(user_name: session[:user_id]).fontColor%>; 
      background: <%= Setting.find_by(user_name: session[:user_id]).background%>;
    }
<% end %>
</style>

</head>

  <body >


<%= yield %>

<%= javascript_include_tag "application" %>
</body>
</html>
